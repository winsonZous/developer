```sql
    SELECT column
    FROM table/view
    JOIN table/View
    ON condition
    WHERE condition
     GROUP BY column
    Having condition
    ORDER BY column desc/esc
    limit (offset,offset)
```
